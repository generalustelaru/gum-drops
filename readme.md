## How to run

1. Start a server by running `npx http-server` from inside `/gum-drops` (it asks for permission to install if nonexistent).
2. Access the app via [http://localhost:8080]() or one of the exposed addresses in the 'Available on' list.