## How to run

1. Start a server by running `npx http-server` from inside `/gum-drops` (it might ask for permission to install).
2. Access the app via [http://localhost:8080](http://localhost:8080) or one of the exposed addresses for local network.